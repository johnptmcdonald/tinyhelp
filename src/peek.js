/**
 * A function to return the last item in an array
 * (array is unchanged)
 * @param {Array} arr - The array whose last item you want to return
 * @return {*} - The last item from the array
 */
const peek = (arr) => arr[arr.length - 1];

module.exports = peek;
